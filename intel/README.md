# intel/ — demand intelligence

- **pantheon_intel.py** — builds the per-title demand feed (cron) → `pantheon_intel.db`, served by `../pantheon_web.py`
- **decoy_detect.py** — flags decoy / fake torrents (cron)
